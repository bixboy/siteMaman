<section class="section cta">
  <div class="cta__inner">
    <h2>Prêts à redonner vie à votre fauteuil ?</h2>
    <p>
      Parlez-moi de votre fauteuil, de vos chaises ou de votre canapé : je vous réponds sous
      48 h avec un devis personnalisé.
    </p>
    <div class="cta__actions">
      <a class="btn" href="/contact">Demander un devis de tapisserie d'ameublement</a>
      <a class="btn btn--outline" href="mailto:mainemerveille@gmail.com">Écrire un email</a>
    </div>
  </div>
</section>
<style>
  .cta {
    background: radial-gradient(circle at top right, rgba(176, 92, 68, 0.15), transparent),
      #1f1c19;
    color: #f4f0ea;
  }

  .cta__inner {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: var(--spacing-lg) var(--spacing-sm);
    display: grid;
    gap: var(--spacing-sm);
    text-align: center;
  }

  h2 {
    font-family: 'Cormorant Garamond', Georgia, serif;
    font-size: clamp(2rem, 4vw, 2.75rem);
    margin: 0;
  }

  p {
    margin: 0 auto;
    max-width: 48ch;
    color: rgba(244, 240, 234, 0.85);
  }

  .cta__actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--spacing-sm);
  }

  .btn--outline {
    color: #f4f0ea;
    border-color: rgba(244, 240, 234, 0.6);
  }

  .btn--outline:hover,
  .btn--outline:focus-visible {
    background: #f4f0ea;
    color: var(--color-text);
  }
</style>
